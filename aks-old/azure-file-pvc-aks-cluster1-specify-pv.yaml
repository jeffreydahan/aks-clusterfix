apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: customer2-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile
  resources:
    requests:
      storage: 5Gi
  volumeName: "pvc-xxx"
